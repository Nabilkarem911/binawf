# تقرير الاختبار — موهبة فنان

تم تشغيل قاعدة البيانات، وتطبيق migrations وseed، ثم تشغيل خادم الإنتاج `npm run start` على `http://localhost:3000`، وتنفيذ اختبارات Playwright المتكاملة.

## البيئة

- Next.js: `16.3.4`
- Prisma: `7.10.0`
- PostgreSQL: `16` (Docker)
- Browser tests: Chromium و Mobile Chrome (Pixel 7)

## أوامر التحقق

```bash
npm run typecheck   # tsc --noEmit
npm run lint        # eslint
npm run build       # next build
npm start           # على المنفذ 3000
npm test            # playwright test
```

## نتائج الاختبار

| الأمر | النتيجة |
|-------|---------|
| `npm run typecheck` | نجح |
| `npm run lint` | نجح |
| `npm run build` | نجح |
| `npm test` | **24 / 24 اختبار نجح** |

## السيناريوهات المختبرة

### الموقع العام

1. **الصفحة الرئيسية**: ظهر العنوان "موهبة فنان"، الاتجاه RTL، النص العربي، قائمة التنقل الرئيسية.
2. **صفحة قسم**: `/national-day` عرضت الأقسام الفرعية (اليوم الوطني ٩٠–٩٥) والمحتوى المنشور.
3. **صفحة مقال**: `/art-education-news/new-website-launch` عرضت عنوان المقال ومحتواه.
4. **البحث**: `/search?q=موهبة` أعاد نتائج مطابقة.
5. **Sitemap**: `/sitemap.xml` يحتوي على `<urlset`.
6. **Robots**: `/robots.txt` يحتوي على `Disallow: /admin`.
7. **404**: مسار غير موجود أعاد حالة 404.
8. **التجاوب المحمول**: عند عرض 375×667، ظهر زر القائمة وعمل عند النقر وظهرت الأقسام داخل الـ sheet.

### لوحة التحكم

9. **تسجيل الدخول**: `/admin/login` مع `admin@binawf.local` / `Binawf2026!` نجح وتوجيه للوحة.
10. **إدارة المحتوى**: إنشاء مقال + تعديله + حذفه عبر `/admin/content`.
11. **إدارة الأقسام**: إنشاء قسم + تعديله + حذفه عبر `/admin/categories`.
12. **رفع وسائط**: رفع صورة PNG 1×1 وظهورها في جدول الوسائط ومعاينة الصورة المصغرة.
13. **إعدادات الموقع**: تعديل عنوان الموقع وانعكاسه مباشرة في رأس الصفحة الرئيسية.

### التحقق العام

- لم تُسجّل أخطاء في Console أو Network (ما عدا الطلبات المتوقعة للـ 404).
- لم تُرصد أي حالة فاشلة في السيناريوهات.

## المشاكل التي وُجدت وأُصلحت أثناء الاختبار

1. **`revalidatePath("/[...slug]")` بدون type**: Next.js 16 تحذّر من أن التوجيه الديناميكي لا يُفريغ cache بدون `type`. تمت إضافة `, "page"` في content/category/settings actions.
2. **زر القائمة المحمول لا يحمل `data-slot` محدد**: تم تحسين محدد الاختبار لاستخدام `[data-slot='sheet-content']`.
3. **صفحة تسجيل الدخول لا تحتوي على `h1`**: تم استبدال `CardTitle` بـ `h1` لأغراض الوصول والاختبار.
4. **حذف المحتوى/القسم لا يعيد التوجيه للقائمة**: تمت إضافة `redirect` في `deletePost` و `deleteCategory`.
5. **الملفات المرفوعة لا تظهر مباشرة في `next start`**: تم تغيير مسار الوسائط ليتم تخزينها في `public/uploads` وخدمتها عبر `GET /api/media?filename=...`، مما يضمن ظهورها دون إعادة تشغيل الخادم.
6. **اعتماديات `package.json` ناقصة**: تمت إعادة `@prisma/adapter-pg` و `pg` و `@types/pg` و `dotenv` و `tsx` و `@playwright/test`.

## الملاحظات

- التحذيرات الأمنية في `npm audit` تبقى في اعتماديات Prisma 7.10.0 (deepmerge-ts، mysql2)؛ لا يمكن حلها دون خفض إصدار Prisma.
- تم التقاط لقطات شاشة لكل اختبار في مجلد `test-results/` (مُدرج في `.gitignore`).
